# Generated by pnpm to record the packages allowed past the local
# `minimumReleaseAge` supply-chain policy. Keep it: without it, `pnpm install`
# on a machine with that policy enabled resolves different versions.
minimumReleaseAgeExclude:
  - lucide-react@1.29.0
  - vite@8.2.1
