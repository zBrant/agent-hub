# Project images

Screenshots used by the root `README.md` live in this directory:

- `dashboard.png`
- `session-graph-and-run.png`
- `code-search.png`

Use PNG for interface screenshots, crop sensitive or irrelevant content, and
keep the images at a readable resolution. Keep these stable filenames when
replacing a screenshot so the documentation links continue to work.
